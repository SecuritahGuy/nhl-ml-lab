# Backend API URL
# Local dev: leave empty (Next.js rewrites proxy to localhost:8000)
# Production (Cloudflare Pages): set to deployed backend URL, e.g.
#   NEXT_PUBLIC_API_URL=https://nhl-ml-lab-backend.onrender.com
# NEXT_PUBLIC_API_URL=
