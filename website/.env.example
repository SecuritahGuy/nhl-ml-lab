# Backend API URL
# Local dev: leave empty (Next.js rewrites proxy to localhost:8000)
# Production (Cloudflare Pages): set to deployed Worker URL, e.g.
#   NEXT_PUBLIC_API_URL=https://nhl-ml-lab-backend.<your-subdomain>.workers.dev
NEXT_PUBLIC_API_URL=
